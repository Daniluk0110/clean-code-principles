# 🧩 Value Objects

## ✅ Что такое Value Object
Value Object — это объект, который описывает характеристику, не имеет identity и определяется только своими данными. Он заменяет **Primitive Obsession**, когда везде используются `string`, `int`, `float` вместо предметных типов.

Value Object дает:
- встроенную валидацию при создании;
- концентрированную бизнес-логику;
- понятные сигнатуры методов;
- невозможность некорректного состояния.

## 🚩 Запахи кода
Если в проекте везде встречаются `string $email`, `float $price`, `int $userId` — это сигнал.

Проблемы:
- нет валидации при создании данных;
- нет поведения (форматирование, сравнение, домен и т.д.);
- логика размазана по всему коду.

## ❌ Было (Плохо)
```php
public function createOrder(int $userId, float $amount, string $email): void
{
    // можно передать мусор в любое поле
}
```

## ✅ Стало (Хорошо)
```php
public function createOrder(UserId $userId, Money $amount, Email $email): void
{
    // валидация происходит при создании объектов
    // некорректные данные невозможны по определению
}
```

## 🧠 PHP 8.4 Asymmetric Visibility — убийца геттеров
Раньше приходилось делать геттеры или объявлять весь объект `readonly`. Теперь можно читать свойство снаружи, но менять только внутри класса.

### ❌ Было (Плохо)
```php
final class Money
{
    private int $amount;

    public function getAmount(): int
    {
        return $this->amount;
    }
}
```

### ✅ Стало (Хорошо)
```php
final class Money
{
    public private(set) int $amount;
}
```

Такое свойство доступно для чтения снаружи, но изменить его может только сам объект. Это делает код чище и убирает лишние геттеры.
